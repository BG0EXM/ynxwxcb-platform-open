# 伊宁县委宣传部部务工作平台

> 版本：V1.4.2

面向县级党委宣传部的内部部务工作平台，开源免费，可自部署。

- 🗂️ 收文管理（呈批单/传阅登记卡/热敏标签打印）、考勤点到（含迟到）、请假管理、公车报备、值守排班、工作日历、会务管理、公共资料、大事记、每周工作总结、加班管理、年休假管理、常委管理、通讯录
- ⚙️ Go + Vue3 + SQLite 单二进制部署，零运维
- 🔒 可配置的角色-功能权限矩阵（管理员/科室/通讯员等），数据范围按科室/本人隔离
- 📜 操作日志审计（仅管理员），关键操作留痕、中文可读
- 🛡️ 登录按用户名+IP 限流、失败锁定、令牌可吊销，密码至少 8 位
- 🔑 首次登录强制修改默认密码，保障账号安全
- 📄 各模块台账一键导出 Excel，支持 A4 打印与 80×50mm 热敏标签
- 📊 列表接口分页，数据量大时依然流畅
- ⚡ 前端按需加载，首屏体积减小约 75%
- 🌐 完全离线可部署，Nginx / Caddy 均可，支持 IPv6

## 功能模块

| 模块 | 说明 |
|---|---|
| 收文管理 | 上级来文登记、呈批单/传阅登记卡/热敏标签生成打印、文件退回管理 |
| 考勤点到 | 管理员晨会手工点到、月度/年度统计与打印 |
| 请假管理 | 年假及各类假期登记、假条一键打印（按请假天数自动匹配审批流程）、统计与 Excel 导出 |
| 公车管理 | 车辆基础信息（车架号/保险等）、用车报备、派车单打印 |
| 会务管理 | 会议录入、生成匿名报名链接、参会登记（单人/多人）、签到单导出 |
| 公共资料 | 共享资料发布与阅读 |
| 通讯录 | 按部门/姓名查询 |
| 值守排班 | 日历视图当天值守（至21:00收文）、可标记县委大院排班 |
| 工作日历 | 全屏大日历（按月/按年展示），各科室在格子中添加要做的工作，跨天连续显示，列表展示，Excel 导出（可单科室/全部） |
| 大事记 | 各科室每月重大事项，按年汇总整个宣传部并导出 Word（仅管理员） |
| 每周工作总结 | 各科室录入本周重点工作，管理员可增删改全部，导出 Word 汇总（仅管理员） |
| 加班管理 | 加班记录录入、加班统计与补休管理（8 小时=1 天），Excel 导出（仅管理员） |
| 年休假管理 | 管理员配置每人每年年休假天数，已休联动请假，普通用户只读（仅管理员可配置） |
| 常委管理 | 常委大事记，按年/月展示，录入/修改/删除，导出 Word（仅管理员） |
| 系统管理 | 用户、角色、部门管理（添加/改名/删除） |
| 权限管理 | 角色-功能权限矩阵，46 个权限点按角色勾选（仅管理员） |
| 操作日志 | 关键操作/登录/导出留痕，按模块/动作/日期/关键词查询（仅管理员） |

> 各模块台账（用车报备/请假/考勤/排班/收文）均支持 Excel 导出。

## 更新日志

### V1.4.2

**新增功能**
- 📋 会务签到单列出全部参会单位：已确认填人员、未确认留空供现场签到、不参加备注"已确认不参加（原因）"
- 🗂️ 会务报名情况弹窗新增"未确认"页签（列出尚未确认参会的单位）
- ✨ 会务报名页：成功提示不再自动消失、刷新/重选已填单位常驻显示、增加"是否需要修改"引导
- 🔐 角色-功能权限矩阵：新增"权限管理"页（仅管理员），46 个权限点对 4 个角色勾选；admin 锁定不可取消；默认等于升级前行为（收文 4 项默认仅 admin）
- 📜 操作日志：记录关键新增/修改/删除/导出与登录（操作人/科室/中文描述/IP/时间），仅管理员可查、只读、自动保留 1 年
- ✅ 敏感操作二次确认：考勤点到、年休假配置、加班录入
- 🗓️ 工作日历权限收紧：各科室只能修改/删除本科室录入的工作，管理员可管理全部
- 🌐 IPv6 支持：应用原生双栈，对外页脚标注"本站支持 IPv6"

**安全加固**
- 🛡️ JWT 密钥：启动拒绝默认值/占位符/短于 32 位，防止令牌被伪造
- 🛡️ 令牌吊销：改密/重置/禁用/改角色/登出后旧令牌立即失效（users.token_version）
- 🛡️ 服务器：读写/空闲超时 + MaxHeaderBytes；非上传接口请求体上限 10MB
- 🛡️ 安全响应头：CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、Permissions-Policy
- 🛡️ 登录：按用户名（5 次锁 10 分钟）+ 按 IP（20 次/分钟）双重限流；不存在用户也执行 bcrypt，消除用户名枚举
- 🛡️ 公开报名接口按 IP 限流（30 次/分钟）
- 🛡️ 密码策略：修改密码至少 8 位
- 🛡️ 可配置 `server.trust_proxy`（前置 Caddy/WAF 时信任 X-Forwarded-For）
- ⬆️ 升级 Go 1.26；依赖更新

**缺陷修复 · 高危**
- 收文列表/详情：空字段（NULL）Scan 失败导致字段错位、详情打不开 → 可空列用 `sql.NullString` 中转
- 值守排班导出：SELECT 5 列却 Scan 6 个目标 → 导出全空白，删除多余参数
- 年休假导出：rows 未关闭就再查询导致死锁、已休天数按行序号错位 → 先读完关闭再查、按 user_id 对应
- 用户无部门/角色：LEFT JOIN 出 NULL 致登录 500 → NullString 中转
- Word 导出：document.xml 缺 `</w:document>` → 非法 XML，补齐闭合
- 前端列表无分页：超过 20 条看不到 → 收文/通讯录/考勤/请假/用车加分页
- 大事记编辑：前端不传 event_type 被覆盖为空 → 后端固定 monthly
- 公共资料附件下载：`<a>` 直链不带 token 必然 401 → 改为带 token 的 blob 下载
- 会务"不限制人数(0)"被当 1 人 → `unit_limit ?? 1`

**缺陷修复 · 中危**
- 越权：传阅记录增删改无校验 → 限办公室部门
- 越权：删除用车报备无校验 → 限本人或管理员（前端按钮同步）
- 越权：删除公共资料无校验 → 限管理员并级联清理附件文件
- 越权：跨科室修改/删除大事记、每周总结 → 加同科室校验
- 首页"本月/全年请假天数"口径与考勤/年假不一致 → 统一 `MIN(登记天数, 重叠整天)`
- 请假修改：可伪造 user_id 改到他人名下、补休可超余额 → 非 admin 强制本人、补休校验余额
- 文件上传无硬性大小限制 → `MaxBytesReader`
- 公开报名接口泄露完整手机号 → 脱敏 `138****0001`（编辑留空保留原号）
- 用车报备：非管理员进入空白标签、按钮文案反 → 修正默认标签与文案
- 加班记录/统计：普通用户可看全员 → 限本人
- 考勤月/年统计无权限限制 → 限管理员
- 考勤月度统计出错路径不关 rows 致连接泄漏 → defer Close
- 可空列 Scan（用车/资料/通讯录/排班/考勤/请假/加班/工作日历/大事记/每周总结/常委/会务/导出）→ NullString + 处理错误
- 多步删除未用事务（收文/车辆/用户/会议/公开报名删+插）→ 事务化
- 删除用户：对不存在的列执行删除、整体无事务 → 移除无效语句 + 事务化
- `resetAutoIncrement` 非事务 → 事务化
- 创建用户 `MAX(id)+1` 竞态 → 单条 `INSERT...SELECT` 原子取号
- 重置密码未校验用户 → 校验 ID 与影响行数
- 会务报名数把"不参加"也计入 → 排除
- 会务手机号校验前后端不一致 → 后端对齐 `1[3-9]\d{9}`
- 公开报名不校验单位属于会议 → 校验
- 值守/收文导出无范围限制 → 按筛选导出

**缺陷修复 · 低危**
- Word 标题字号小于正文 → 调整层级
- 收文更新未校验标题非空 → 补校验
- COUNT/Scan 错误被忽略 → 处理
- 删除车辆非事务 → 事务化
- 用车详情无归属校验 → 限本人或管理员
- 资料分类先查后插竞态 → 靠唯一约束兜底
- 排班日期格式未校验 → 校验 YYYY-MM-DD
- 首页今日值守只显示一人 → 支持多人
- 附件下载无归属校验、硬编码路径 → 校验 + 用配置目录
- 角色列表 Scan 可空 → 处理
- 请假记录 Scan 可空 reason → 处理
- 年统计已删用户记录被静默丢弃 → 可空处理
- 考勤点到批量无校验/无事务 → 校验状态+人员 + 事务
- 请假类型未白名单、日期未校验 → 校验
- 非管理员调用考勤统计报错 → 前端按权限隐藏
- 年休假列表/导出 Scan、Err、年份格式 → 处理 + 校验
- 加班：补休剩余统计页与登记校验口径不一致 → 统一全时口径；录入日期/小时校验；错误处理
- 工作日历：更新未校验 start_date、导出日期格式不统一、缺日期校验 → 修复
- 大事记/每周总结：无科室用户越权、period 格式、导出显示"0月" → 修复
- 会务：取消报名不校验影响行数、rows.Err 未检查、删除多步无事务、录入缺日期/单位校验 → 修复
- 导出：Scan 错误、日期格式不统一、忽略筛选（收文/请假/工作日历）→ 修复
- 前端：收文"未退/不需要退回(0)"筛选无效；非管理员误调统计；编辑用户强制启用；会务菜单对非管理员显示；大事记编辑改科室无效；Dashboard 考勤状态缺迟到/培训；Study 取消发布后表单残留；router `JSON.parse` 无保护；Overtime 整页跳转；LeavePrint 只查前 100 条；按钮文案反；非管理员默认空白标签 → 修复

**数据库**
- 🗄️ 迁移 v12（操作日志）/ v13（权限矩阵）/ v14（令牌版本）

### V1.4.1

- ⏱️ **请假支持按小时/半天**：登记可选"按天 / 按小时"，按小时填 0.5~8 小时自动折算天数（8 小时=1 天），存 leave_hours
- 🏷️ **新增培训类型**：请假类型加"培训"；考勤点到新增"培训"状态（status=6），日/月/年统计、打印、Excel 导出同步
- 🔍 **请假记录年月筛选**：记录列表可按某年某月筛选，导出跟随
- 🐛 **考勤统计半天假修复**：请假统计改 `MIN(days, 重叠整天)`，半天/小时假（0.5/0.25）精确显示，不再四舍五入
- 🐛 **联动口径统一**：年休假已休、补休已用、请假统计、年休假导出统一按 `MIN(days, 重叠)`，半天/小时假精确扣减
- 🐛 **跨年假归属**：请假统计按当年实际覆盖天数拆分（与年休假/考勤一致）
- 🐛 **请假日期校验**：登记/修改后端校验结束日期不早于开始日期
- 🐛 **会务修复**：未填时间的会议当天仍可报名（截止当天 23:59:59）；过期会议禁止取消报名
- 🗄️ 数据库迁移 v11：请假表加 leave_hours

### V1.4.0

- 📋 **新增会务管理**：录入会议（标题/时间/地点/参会单位范围/参会人数），生成报名链接发送各单位
- 📥 **匿名报名页**：各单位无需登录即可报名参会（姓名/职务/电话），支持单人/多人与会、单位可确认不参加、报名截止后自动失效
- 🔄 **报名可反悔**：同单位可改报/移除参会人，不参加可改为参加
- 📄 **签到单导出**：一键导出含报名人员信息的 Excel 签到单（线下签到用）
- 🐛 **修复删除用户**：删除用户时一并清理各新模块表记录（日历/大事记/每周总结/加班/年休假/常委/公共资料/会务），避免残留
- 🎨 **报名页响应式**：适配手机/平板/桌面
- 🗄️ 数据库迁移 v9（会务表）、v10（参会人数上限）

### V1.3.9

- 🗂️ **菜单分组**：侧边栏按"业务办理 / 考勤休假 / 材料报送 / 系统管理"分组，工作台置顶
- 🏖️ **新增年休假管理**：管理员配置每人每年年休假天数，按人展示"配置/已休/剩余"（已休联动请假年假），普通用户只读，Excel 导出
- 🔓 **权限调整**：加班管理、年休假管理普通用户可查看，仅管理员可录入/配置/导出
- 🏷️ **考勤补休**：补休期间考勤点名自动按出勤处理，姓名旁显示"补休"标记
- 🔧 修复考勤点到请假类型下拉出现灰色 "comp"
- 🗄️ 数据库迁移 v8：新增年休假配置表

### V1.3.8

- 🐛 **修复加班统计姓名/部门为空**：无加班记录的用户不再显示空白行（user_id 从用户表取值）
- 📆 **加班记录按年/月筛选**：默认显示当年全部加班记录，可按年、按月筛选，导出跟随筛选（默认导出当年）

### V1.3.7

- 🗂️ **收文密级选项补全**：非密/内部/秘密★各级别/机密★各级别共 17 项，列表与导出同步显示
- 📅 **导出日期格式**：Excel 导出日期改为斜杠格式（如 2025/9/2）
- 🧾 **收文编号**：移除输入框灰色示例文字
- 📋 **大事记**：填写框放大为多行，移除详情字段
- ⏱️ **新增加班管理**：加班记录录入、加班统计与补休管理（8 小时=1 天）、可补休为 0 时禁止登记补休、Excel 导出
- 🏖️ **请假新增类型**：产检假、探亲假、补休；登记时校验同人日期重叠并提醒
- 🎨 **UI 现代化**：全局主题（党政红主色+墨蓝渐变侧边栏+毛玻璃登录页+卡片圆角阴影）、紧急/特急文件打印加红色标记
- 🔧 所有数据库遍历循环补 `rows.Err()` 检查（12 个文件 40 处），遍历中途出错不再静默返回空数据
- 🗄️ 数据库迁移 v6（加班表）、v7（清理废弃列）

### V1.3.6

- 🆔 **用户 ID 连续**：新增用户按当前最大 ID + 1 分配，删除后不再跳号
- 📅 **新增工作日历**：全屏大日历（按月/按年），各科室在日期格子中添加要做的工作，跨多天工作连续显示；列表展示；Excel 导出（可单科室/全部）；权限全开
- ⏰ **考勤迟到**：点到新增"迟到"状态，月度/年度统计同步增加迟到统计
- 👥 **新增常委管理**：常委大事记，仅记录常委个人情况，按年/月展示，录入/修改/删除，导出 Word（仅管理员）
- 📋 **周月年报重做为大事记**：废弃周/月/年报，改为各科室每月重大事项；支持按年汇总整个宣传部并导出 Word（仅管理员，按月公文格式排版）；旧 reports 表与代码全部清理
- 📝 **新增每周工作总结**：各科室录入本周重点工作，可自行增删改；管理员可增删改全部；导出 Word 汇总所有科室（抬头单位名称+周数+各科室依次列出，仅管理员）
- 📱 **响应式布局**：全平台适配不同屏幕与移动端，侧边栏移动端抽屉式收起
- 🌐 **日历汉化**：全平台日期选择器/日历切换为中文
- 🗄️ 数据库迁移 v5：新增工作日历/常委大事记/大事记/每周工作总结四张表，废除 reports 表

### V1.3.5

- ✏️ **请假信息可修改**：提交人本人或管理员可编辑请假记录
- 🚗 **用车报备可修改**：报备人本人或管理员可编辑已报备项目
- 👔 **新增分管领导角色**：可审阅报表，权限较大；考勤点到仍由系统管理员使用
- 📅 **考勤日历标色**：已点到日期标绿、未点到标红
- 📆 **值守排班日历**：改以星期一为首列
- 🗄️ 数据库迁移 v4：新增分管领导角色

### V1.3.4

- 🐛 **收文编辑不再弹出详情**：修复编辑按钮事件冒泡问题
- 📚 **公共资料**：新增删除功能、分类名称管理
- 📊 **考勤月度统计**：跨月请假按当月实际天数计算，各月准确拆分
- 📊 **考勤年度统计**：按人员显示全体干部全年各类休假情况

### V1.3.3

- 🐛 **请假条打印部门显示**：请假列表补部门联查，假条部门不再空白
- 🐛 **考勤全部出勤不再覆盖请假**：请假人员保持请假状态
- 🔧 **考勤点到显示优化**：自动请假用户标记高亮，已保存结果可修改
- 🐛 **值守排班备注可编辑**：备注可在对话框内修改，并显示在日历格子上
- 🔧 **用车报备支持科室人开车**：新增"开车人"字段，选车自动带出司机
- 📄 **周月年报分页 + 按年筛选**
- 🐛 **公车管理"只看我的"**：管理员默认查看全部，可切换
- 📝 **报表可修改/删除**：提交人本人或管理员可编辑、删除报表

### V1.3.2

- 🖨️ **修复传阅登记卡打印排版**：表格改用弹性布局填充 A4 页面，底部平台信息不再被挤到第二页

### V1.3.1

- 🛠️ **修复部门删除后 ID 不复用**：删除部门时重置自增序列，新添加的部门可复用被删除的 ID，避免 ID 持续增大

### V1.3

- 🗓️ **考勤点到自动识别请假**：管理员晨会点到时，若人员当日已登记请假（请假日期区间覆盖当天），自动显示为"请假"状态并带出请假类型，避免误点
- 🙋 **普通用户自行请假**：普通用户可自助提交请假申请（自动绑定本人账号），管理员仍可代任意人员登记；请假记录与假条打印对本人开放
- 🧹 清理数据库残留文件

### V1.2

- 🛡️ **登录限流**：同一账号连续 5 次登录失败后锁定 10 分钟，防暴力破解
- 🔒 **报表审阅权限**：报表审阅仅限管理员，普通用户只能提交/查看自己的报表
- 📊 **列表分页**：收文、通讯录、请假、用车报备、报表、考勤列表均支持分页，接口返回 total/page 供前端翻页
- ⚡ **前端按需加载**：Element Plus 按需引入，主包体积从约 1.2MB 降至约 290KB，首屏加载更快
- 🔄 **顶栏待办刷新改事件驱动**：收文登记/删除后实时刷新待办数，不再 60 秒轮询
- 🗄️ **数据库版本号迁移机制**：`schema_versions` 表记录数据库版本，启动时按版本号顺序自动执行未执行的迁移，升级不丢数据
- 📋 **请假假条一键打印**：A4 请假条打印（含申请人签字/领导审批栏），按请假天数自动匹配审批流程（半天内→科室主任；1-3天→科室主任+分管领导；3天以上→科室主任+分管领导+县分管领导），请假天数随起止日期自动计算
- 🏢 **部门管理**：用户管理中可添加、改名、删除部门，删除时自动校验关联（部门下有用户/通讯录时禁止删除），防止数据孤立
- 🧹 清理无效代码（CreateUser 冗余空判断）

### V1.1

- 🔑 **首次登录强制改密**：使用默认密码登录后，强制要求修改密码，未修改前无法使用其他功能
- 🐛 **修复用户/通讯录部门显示空白**：部门列字段名修正

## 技术栈

- 后端：**Go 1.25**（标准库 HTTP）+ SQLite（纯 Go，无 CGO）
- 前端：**Vue 3 + Element Plus + Pinia + Vue Router + Vite**
- Excel 导出：excelize/v2
- 部署：主程序 + 静态资源目录，反向代理（Nginx / Caddy 均可），systemd 托管

## 目录结构

> 下面描述的是**源码仓库**的结构。部署到服务器时，只需取出「可执行文件 + static 文件夹」两样（见下方「部署」章节）。

```
├── backend/                 # Go 后端
│   ├── cmd/server/          # 入口
│   ├── internal/            # 后端业务代码（auth/config/database/handlers/...）
│   └── static/              # 前端静态资源（部署时取出来，与主程序放一起）
├── frontend/                # Vue 前端源码
│   └── src/
│       ├── views/           # 各业务页面
│       ├── router/          # 前端路由
│       ├── store/           # Pinia 状态
│       └── utils/           # axios 封装 + 导出下载
├── config.json.example      # 配置模板
└── docs/                    # 部署与架构文档
```

## 快速开始

### 后端

```bash
cd backend
cp ../config.json.example config.json   # 修改密钥与管理密码
go run ./cmd/server -config config.json
# 默认监听 :8080，首次运行自动建库
```

> `backend/static/` 已内置前端构建产物，**开箱即用**，无需再构建前端。

### 修改前端后重新构建

```bash
cd frontend
npm install
npm run build
# 将 dist 复制回 backend/static：
rm -rf ../backend/static && mkdir -p ../backend/static && cp -r dist/* ../backend/static/
```

### 交叉编译（Linux amd64）

```bash
cd backend
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o ynxwxcb-server ./cmd/server
```

## 部署

系统由两部分组成，**都要放到服务器上**：
1. **主程序** `ynxwxcb-server`（编译出的可执行文件）
2. **static 文件夹**（前端网页文件）

部署后服务器目录结构如下：

```
/opt/ynxwxcb/
├── ynxwxcb-server        # 可执行文件
├── static/             # 前端页面文件夹（与可执行文件同级）
├── config.json         # 配置文件
└── data/               # 运行时自动生成（数据库/上传文件）
```

**部署有两种方式，任选其一：**

---

### 方式一：本地编译好后上传（推荐，最简单，服务器不用装环境）

> 适合小白，你只需要在自己电脑上做两步，然后把两个东西传到服务器。

**第 1 步：在自己电脑上编译出可执行文件**

在电脑上打开终端（命令行），进入项目 backend 目录：

```bash
cd backend
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o ynxwxcb-server ./cmd/server
```

> 前提：电脑上装了 Go 语言环境。没装的话，看文末「常见问题」里的安装方法。
> 如果你连 Go 都不想装，也可以找已编译好的 `ynxwxcb-server` 文件直接用。

**第 2 步：把两个东西传到服务器**

需要上传到服务器的就这两样（放进同一个目录，比如 `/opt/ynxwxcb/`）：

| 文件/文件夹 | 说明 |
|---|---|
| `ynxwxcb-server` | 编译出的可执行文件 |
| `static/` | 前端页面文件夹（用仓库 `backend/static/` 里的） |

**第 3 步：在服务器上运行**

```bash
cd /opt/ynxwxcb
./ynxwxcb-server -config config.json
```

> 用 systemd 托管可开机自启，见 `deploy/systemd/ynxwxcb.service`。

---

### 方式二：在服务器上直接编译（懂行的用，服务器要装 Go）

> 适合在服务器上有 Go 环境、且把源码传到服务器的人。

```bash
# 1. 把整个项目源码上传到服务器（含 backend、frontend 等）
# 2. 进入 backend 目录编译
cd backend
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o ynxwxcb-server ./cmd/server

# 3. 把 static 文件夹放到与 ynxwxcb-server 同一目录，然后运行
./ynxwxcb-server -config config.json
```

---

> **关键点**：后端在**运行目录**下找 `static/` 文件夹，所以无论用哪种方式，`ynxwxcb-server` 和 `static/` 必须放在**同一目录**。仓库里的 `backend/static/` 已帮你准备好前端页面，直接把它作为服务器上的 `static/` 文件夹用即可。

**什么时候需要重新构建前端？**
只有当你想**修改前端界面**（换背景图、改文案、改界面布局）时，才需要用 `npm run build` 重新生成 `static/` 文件夹。不想改前端就完全不用管。

完整部署见 [docs/DEPLOY.md](docs/DEPLOY.md)（Nginx）或 [docs/DEPLOY-CADDY.md](docs/DEPLOY-CADDY.md)（Caddy）。

## 权限模型

- **管理员（admin）**：全部功能 + 用户管理
- **科室工作人员（staff）**：业务办理
- **乡镇/通讯员（reporter）**：投稿、接收通知、上报材料

> 收文的新增/编辑/删除仅限"办公室"部门用户，其他部门只读。

## 数据库升级

从旧版本升级到新版本，**只需替换主程序 `ynxwxcb-server` 后重启**，无需手动操作数据库。

- 程序启动时会自动检测数据库版本（`schema_versions` 表），按版本号顺序执行未执行的迁移
- 新增表 / 加字段 / 改结构都通过迁移自动完成，**已有数据完整保留**
- 升级前建议先备份：`bash backup.sh`（备份到 `/opt/ynxwxcb-backup/`）

> 迁移逻辑见 `backend/internal/database/database.go` 的 `migrations` 列表，每次版本升级只需追加一个迁移函数。

## 默认账号

首次部署后：`admin` / `admin123`。

> **安全机制**：使用默认密码 `admin123` 或初始密码 `123456` 登录后，系统会**强制要求修改密码**，未修改前无法使用其他功能。请登录后立即设置新密码。

## 常见问题

**Q1：电脑没装 Go，怎么编译？**
- Windows：去 https://go.dev/dl 下载安装包，装完重启终端
- Linux：`sudo apt install golang`
- 如果实在不想装 Go，可以让已编译好的人给你一份 `ynxwxcb-server` 文件直接上传

**Q2：`CGO_ENABLED=0 GOOS=linux GOARCH=amd64` 是什么？**
- 表示"编译出 Linux 64 位系统能运行的程序"（不管你在 Windows 还是 Mac 上编译）
- 照抄这行命令即可，不用理解每个参数

**Q3：上传到哪里？**
- 可执行文件和 static 文件夹放**同一个目录**，建议 `/opt/ynxwxcb/`

**Q4：static 文件夹哪来的？**
- 直接用仓库里的 `backend/static/` 文件夹，整文件夹复制到服务器

**Q5：页面打开是 404 或空白？**
- 99% 是 static 文件夹没放对位置或没传上去
- 确认服务器上 `ynxwxcb-server` 和 `static/` 在**同一目录**

## License

[Apache License 2.0](LICENSE)

## 声明

- 项目中的党徽图片为官方标识，仅供授权单位内部使用，部署者须遵守相关规定
- 本项目用于单位内部管理，部署时请遵循当地网络安全与保密要求
- 登录页与主页面底部预置了"ICP备案号占位 / 公网安备号占位"，公网部署者请替换为自己的真实备案号（在 `frontend/src/views/Login.vue` 与 `Layout.vue` 中修改后重新构建）
